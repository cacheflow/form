var React = require('react');

var SchedulerForm = React.createClass({
  propTypes: {
    inputText: React.PropTypes.string,
    dynoText: React.PropTypes.string,
    dueText: React.PropTypes.string
  }
});


key = {index + formData.dueText}
inputText = {formData.inputText}
dynoText = {formData.dynoText}
dueText = {formData.dueText}

module.exports = SchedulerForm;
